# Grafana datasource provisioning: point Grafana at the Prometheus service.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    uid: eta-prometheus
    url: http://prometheus:9090
    isDefault: true
    editable: true
